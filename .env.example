# Backend API URL (Optional)
# The application works with mock data by default if no backend is available
# If you have a backend API, set this to your backend URL
VITE_API_URL=http://localhost:3001/api
